{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_OUTPUT\" | grep -qE '^\\[[a-zA-Z0-9/_-]+ [a-f0-9]+\\]'; then git push origin HEAD 2>&1 || true; fi"
          }
        ]
      }
    ]
  }
}
