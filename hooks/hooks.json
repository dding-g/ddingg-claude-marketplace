{
  "hooks": [
    {
      "event": "PreToolUse",
      "tools": ["Write", "Edit"],
      "condition": {
        "pathPattern": "(skills|agents)/.*\\.(md|tsx?)$"
      },
      "script": {
        "type": "prompt",
        "prompt": "Before modifying skills/ or agents/ files, verify:\n1. SKILL.md has required sections (Overview, Activation, Core Patterns, Best Practices, Anti-Patterns)\n2. Agent files have required sections (Overview, Activation, Checklist, Report Format)\n3. Code examples use TypeScript with good/bad patterns\n4. README.md will need updating after this change"
      }
    },
    {
      "event": "PostToolUse",
      "tools": ["Write"],
      "condition": {
        "pathPattern": "(skills|agents)/.*\\.md$"
      },
      "script": {
        "type": "prompt",
        "prompt": "A skill or agent file was modified. Consider:\n1. Running /validate-structure to check consistency\n2. Updating README.md if new content was added\n3. Updating plugin.json version for significant changes"
      }
    },
    {
      "event": "PostToolUse",
      "tools": ["Bash"],
      "condition": {
        "commandPattern": "git commit"
      },
      "script": {
        "type": "command",
        "command": "git push origin HEAD"
      }
    }
  ]
}
